# Vibe Kanban Environment Variables

# ============================================
# API Security (Backend)
# ============================================

# Enable API authentication (disabled by default for backwards compatibility)
# Set to "true" to require Clerk JWT tokens for protected endpoints
# ENABLE_API_AUTH=true

# Clerk domain for JWT validation (NOT your frontend domain!)
# This is the Clerk service domain from your publishable key
# Example: if your key is pk_test_d2VsY29tZS1zd2lmdC03Ni5jbGVyay5hY2NvdW50cy5kZXYk
# then CLERK_DOMAIN=welcome-swift-76.clerk.accounts.dev
# CLERK_DOMAIN=your-subdomain.clerk.accounts.dev

# ============================================
# GitHub OAuth (for repository sync)
# ============================================
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ============================================
# Turso Distributed Database (optional - enables team sync)
# ============================================
# Get these from: turso db show <db-name> --url and turso db tokens create <db-name>
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# Turso sync interval in seconds (default: 60, set to 0 for manual sync only)
# TURSO_SYNC_INTERVAL=60
