# Fly.io configuration for Scho1ar Frontend
app = 'scho1ar'
primary_region = 'sin'

[build]
  dockerfile = "Dockerfile.frontend"
  [build.args]
    VITE_CLERK_PUBLISHABLE_KEY = "pk_test_d2VsY29tZS1zd2lmdC03Ni5jbGVyay5hY2NvdW50cy5kZXYk"
    VITE_API_URL = "https://vibe-kanban-api.fly.dev"
    VITE_BASE_PATH = "/"

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
