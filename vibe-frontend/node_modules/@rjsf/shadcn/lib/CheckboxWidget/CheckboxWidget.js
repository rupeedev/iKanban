import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { ariaDescribedByIds, descriptionId, getTemplate, labelValue, schemaRequiresTrueValue, } from '@rjsf/utils';
import { Checkbox } from '../components/ui/checkbox.js';
import { Label } from '../components/ui/label.js';
/** The `CheckBoxWidget` is a widget for rendering boolean properties.
 *  It is typically used to represent a boolean.
 *
 * @param props - The `WidgetProps` for this component
 */
export default function CheckboxWidget(props) {
    const { id, htmlName, value, disabled, readonly, label, hideLabel, schema, autofocus, options, onChange, onBlur, onFocus, registry, uiSchema, className, } = props;
    // Because an unchecked checkbox will cause html5 validation to fail, only add
    // the "required" attribute if the field value must be "true", due to the
    // "const" or "enum" keywords
    const required = schemaRequiresTrueValue(schema);
    const DescriptionFieldTemplate = getTemplate('DescriptionFieldTemplate', registry, options);
    const _onChange = (checked) => onChange(checked);
    const _onBlur = () => onBlur(id, value);
    const _onFocus = () => onFocus(id, value);
    const description = options.description || schema.description;
    return (_jsxs("div", { className: `relative ${disabled || readonly ? 'cursor-not-allowed opacity-50' : ''}`, "aria-describedby": ariaDescribedByIds(id), children: [!hideLabel && description && (_jsx(DescriptionFieldTemplate, { id: descriptionId(id), description: description, schema: schema, uiSchema: uiSchema, registry: registry })), _jsxs("div", { className: 'flex items-center gap-2 my-2', children: [_jsx(Checkbox, { id: id, name: htmlName || id, checked: typeof value === 'undefined' ? false : Boolean(value), required: required, disabled: disabled || readonly, autoFocus: autofocus, onCheckedChange: _onChange, onBlur: _onBlur, onFocus: _onFocus, className: className }), _jsx(Label, { className: 'leading-tight', htmlFor: id, children: labelValue(label, hideLabel || !label) })] })] }));
}
//# sourceMappingURL=CheckboxWidget.js.map